{:id :EACL-FORMAL-008
 :status :fixed
 :discovered-by :differential
 :seed-or-trace "cache/proof-provider-exception"
 :minimized-fixture "fixture.edn"
 :impact :availability
 :affected-versions ["current worktree at discovery"]
 :affected-backends [:shared :datomic :datascript :datahike :third-party]
 :root-cause
 "cache/resolve! invoked adapter schema-proof and relation-proof callbacks before entering any provider-failure boundary. An exception escaped the cache and aborted authorization instead of treating cache validation as unavailable and recomputing."
 :fix
 "The final v8 cache removes the external proof-provider resolution surface. Current-generation and managed native cache paths own their bounded proof reads; the deleted eacl.cache/resolve! provider callback path cannot abort authorization."
 :closing-evidence
 ["eacl.datomic.trusted-surface-audit-test/deleted-trusted-surfaces-stay-deleted-test"
  "eacl.formal.executed-mutation-controls/cache-fail-open-killed?"]}
