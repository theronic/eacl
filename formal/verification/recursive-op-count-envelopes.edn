{:version 1
 :purpose
 "Logical-work envelopes for the populated-recursion op-count gates
  (eacl-v8-root-fixes, recursion-performance-gates capability).

  Envelopes are recorded at CURRENT TRUTH on the audited tree and are
  RATCHETS: each fix PR tightens the relevant number instead of editing
  test code. Counters are named exactly as bound by the tests:
  eacl.verified-kernel/*kernel-crossing-stats*,
  eacl.backend.v8/*backend-op-stats*,
  eacl.engine.v8/*recursive-traversal-stats*.

  Latency baselines live separately (explorer-v8-recursive-performance.edn,
  task 1.4); logical work remains authoritative per FORMAL-048."
 :recorded {:host-class :apple-silicon-dev
            :eacl-commit "8b17ee0"
            :backend :datascript
            :fixture {:shape :star :accounts 2000}}
 :work-envelopes
 {:raw-lookup-first-50
  {:maximum-schema-proof-reads 3   ; memoized invocations (cheap): route
                                   ; validation + actual + certificate
                                   ; identities all deref one delay
   :maximum-schema-proof-computations 1 ; 3.1 ratchet: one scan per request
   :maximum-plan-compiles 1        ; 3.3 ratchet: nil-store lookup deleted the duplicate
   :maximum-denotation-key-builds 0    ; 3.3 ratchet: no key work without a store
   :maximum-denotation-dependency-calcs 0 ; 3.3 ratchet
   :maximum-permission-path-calcs 11   ; engine-direct raw (no request
                                       ; context bound); the Datomic facade
                                       ; path measures 1 via 3.2's memo.
                                       ; 10->11 recorded by 8.4: plan
                                       ; compilation now cross-checks the
                                       ; compiled rules against the managed
                                       ; dependency closure (one closure
                                       ; walk per engine-direct compile;
                                       ; memoized to zero under any schema
                                       ; cache)
   :maximum-backend-scans 64}      ; demand semantics: first 50 plus sentinel
  :raw-can
  {:maximum-schema-proof-reads 0
   :maximum-plan-compiles 1
   :maximum-denotation-key-builds 0
   :maximum-denotation-dependency-calcs 0
   :maximum-backend-scans 8}       ; bounded reverse point check
  :client-lookup-first-50
  {:maximum-schema-proof-reads 5   ; memoized invocations (cheap)
   :maximum-schema-proof-computations 1 ; 3.1 ratchet: one scan per request
   :maximum-plan-compiles 1
   :maximum-backend-scans 64       ; first 50 plus sentinel
   :maximum-later-page-scans 64}   ; retained continuation: next 50 + sentinel
  :client-can
  {:maximum-schema-proof-reads 2   ; cold client (fresh schema-cache registry);
                                   ; a warmed client reads 1
   :maximum-kernel-drives 8}       ; target-anchored reverse Boolean demand
  :count-full
  {:maximum-derived-grants-factor 2 ; derived-grants <= factor * accounts
   :maximum-backend-scans-slack 50} ; scans <= accounts + slack
  :crossing-law
  {:protocol-version 2
   :batch-size 64
   :constant 40
   :fuel 256}}                     ; star: crossings <= 2*ceil(scans/B)+C+fuel yields
 :notes
 ["Point, bounded count, and page envelopes are independent demands. A cache
   miss never widens any of them to a complete denotation."
  "Continued pages use retained private generated state when present and
   deterministic authenticated-prefix replay when absent."]}
