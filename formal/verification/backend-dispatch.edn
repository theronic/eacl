{:schema-version 1
 :status :dispatch-key-closure-established
 :assurance :static-literal-operation-closure-not-adapter-semantic-proof
 :source-roots
 ["modules/eacl/src"
  "modules/eacl-datomic/src"
  "modules/eacl-datahike/src"
  "modules/eacl-datascript/src"
  "modules/eacl-datalevin/src"]
 :runtime-passes
 {:clj
   {;; 82 -> 52 (2026-08-23): source selection and lifecycle operations moved
    ;; out of the immutable adapter dispatch surface into eacl.backend.source.
    ;; The new :basis-kind observation closes exact identity without granting
    ;; the adapter source authority. Both reader passes retain the same closed
    ;; basis-adapter operation set.
    ;; 52 -> 49 (2026-08-24): stable checkpoint keys moved from native
    ;; revisions to proof-equivalent authorization frames, removing three
    ;; request-path revision observations without changing the closed operation
    ;; set.
    ;; 49 -> 61 (2026-08-26): expression-native planning and permission-tree
    ;; rendering added literal
    ;; :permission-expression read and demand-driven membership batching added
    ;; :direct-match-many?. Both are closed adapter capabilities and appear in
    ;; the CLJ and CLJS reader passes.
    ;; 61 -> 62 (2026-08-26): the Datalevin at-least-as-fresh acquisition
    ;; reads the candidate's native revision before deciding to wait,
    ;; closing the busy-wait finding without widening the operation set.
    ;; 62 -> 63 (2026-08-27): speculative snapshot construction captures the
    ;; committed root schema generation before applying a local native report.
    ;; This is an existing literal :schema-generation capability.
    ;; 63 -> 62 (2026-08-29): exact answer-cache metadata now reuses the
    ;; selected basis's already validated backend snapshot identity instead
    ;; of crossing the immutable adapter boundary a second time.
    ;; 62 -> 49 (2026-08-29): repeated ordered scans and scalar membership
    ;; checks capture immutable implementations once. Their fixed-arity
    ;; invokers retain request metering, observers, runtime guards, typed
    ;; failures, and the same literal operation-key closure.
    ;; 49 -> 48 (2026-08-30): derived-schema cache construction now receives
    ;; explicit lifecycle/basis identity and no longer falls back to a second
    ;; adapter :snapshot-id observation.
    :invoke-call-count 48
   :operations
   #{:snapshot-id
     :basis-kind
     :native-revision
     :order-hint
     :exact-locator
     :object-id->internal
     :internal-id->object
     :relation-defs
     :permission-defs
     :subject->resources
     :resource->subjects
     :direct-match?
     :direct-match-many?
     :permission-expression
     :all-permission-nodes
     :schema-generation
     :proof-frame}}
  :cljs
  {:invoke-call-count 48
   :operations
   #{:snapshot-id
     :basis-kind
     :native-revision
     :order-hint
     :exact-locator
     :object-id->internal
     :internal-id->object
     :relation-defs
     :permission-defs
     :subject->resources
     :resource->subjects
     :direct-match?
     :direct-match-many?
     :permission-expression
     :all-permission-nodes
     :schema-generation
     :proof-frame}}}
 :requirements
 {:every-invoke-operation-is-a-literal-keyword true
  :observed-equals-certified-snapshot-operations true
  :make-adapter-requires-callable-operation-implementations true}
 :adapter-capability-coverage
 {:datalevin
  {:advertised-cache-proofs
   #{:ordered-generations :snapshot-bound :database-visible}
   :required-dispatch-operations #{:schema-generation :proof-frame}
   :implementation
   "modules/eacl-datalevin/src/eacl/datalevin/backend.cljc"
   :certification
   "formal/verification/adapter-certification.edn"}}
 :remaining
 [:backend-operation-semantics-remain-basis-adapter-obligations
  :storage-engine-and-host-runtime-remain-trusted]}
