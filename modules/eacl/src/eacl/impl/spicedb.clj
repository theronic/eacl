(ns eacl.impl.spicedb
  (:require [eacl.core :as proto]))

; todo: transfer SpiceDB gRPC Clojure implementation from closed-source.
; this impl. also satisfies the `IAuthorization` protocol in eacl.protocols.