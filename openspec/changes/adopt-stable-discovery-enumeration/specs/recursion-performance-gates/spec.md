# recursion-performance-gates Specification

## REMOVED Requirements

### Requirement: Matched-v7 latency bound
**Reason**: Recorded `:not-applicable`; see the change's tasks and `formal/verification/explorer-v7-performance.edn`.

**Migration**: None. The requirement described a mechanism the stable-discovery engine removed; consumers see the `:stable-edge` contract in `stable-discovery-enumeration`.
