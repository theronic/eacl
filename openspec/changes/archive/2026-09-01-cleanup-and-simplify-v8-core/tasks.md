Conventions for every task: run tests via nREPL only (AGENTS.md); after any edit under a public source root, run `bin/formal source-closure`; use `clj-paren-repair` for delimiter errors; "battery" = the CI-equivalent runner over the four module test roots + `src-build` excluding `:benchmark`/`:formal-artifact`. A tranche editing `.cljc` engine/operator/kernel files additionally closes with the portable-kernel CLJS differential suite. Reference code by grep-anchored symbols, not memorized line numbers — files move. Never `git add`/`commit` in a dirty checkout without explicit instruction.

## 1. Preflight and baselines

- [x] 1.1 Rebase-check against in-flight changes: diff the files this change touches (engine/, operator/, cache*, verified_kernel, formal/) against `standardize-authorization-cache-storage` and `adopt-stable-discovery-enumeration` task lists; record any overlap and sequencing notes in the change dir. Confirm the git workflow before the first commit: current branch, working-tree state, and the user's branch/commit policy for this checkout. Verify: overlap note exists and names concrete files or states "none"; branch/commit policy recorded.
- [x] 1.2 Start nREPL with `:dev:test:cljs-test`, run the full battery, and confirm green before any edit. Verify: runner reports 0 failures/errors; record the summary counts as the baseline.
- [x] 1.3 Run `bin/formal source-closure` and `bin/reflection-gate` clean on the unmodified tree. Verify: both exit 0.
- [x] 1.4 Capture performance baselines for touched paths: run the routing-certificate and consistency-boundary gate suites plus one `^:benchmark` pagination suite; save outputs under `target/` (ignored). Verify: baseline files exist and are referenced by path in a note in the change dir.

## 2. Repo and tooling hygiene

- [x] 2.1 Add `states/` to `.gitignore`; delete the untracked `states/` TLC dump (937 MB). Verify: `git status` no longer shows `states/`; `du` confirms reclaim.
- [x] 2.2 Remove the stale nested repo copy under `openspec/changes/persist-eacl-cache-s3-express-lambda-openspec/worktrees/`: check `git worktree list` first; if registered use `git worktree remove`/`prune`, else delete the directory. Verify: `git worktree list` clean; directory gone; repo-wide grep for a known phantom (`revision-checkpoints`) no longer hits the copy.
- [x] 2.3 Add `.clj-kondo` config (hook or `:lint-as`) for `eacl.datomic.impl/with-request-engine` binding form. Verify: `clj-kondo --lint modules/eacl-datomic/src` reports zero "Unresolved symbol: adapter" errors and no new suppressions of real linters.
- [x] 2.4 Fix the 22 outstanding kondo warnings at source: unused bindings (`backend/source.cljc:347`, `engine/v8.cljc:601-603` unused `registry`/`schema-generation` arities' params, `request/context.cljc:111`, `subproblem_cache.cljc:599-603`, `client/orchestration.cljc:1412,1691`, `datalevin/fork.cljc:10`), unused private vars handled in group 3, `(not (empty? …))` handled in 5.6. Verify: fresh kondo run over all five src roots reports zero warnings.

## 3. Dead code removal

- [x] 3.1 Delete `modules/eacl-datalevin/src/eacl/datalevin/integrity.cljc` (orphan byte-copy, zero references). Verify: `grep -rn "datalevin.integrity" modules formal bin docs` returns nothing; battery green.
- [x] 3.2 Delete `modules/eacl-datomic/src/eacl/datomic/codec.clj` and `modules/eacl-datomic/test/eacl/datomic/codec_test.clj`; note the removal in the module README/changelog. Verify: repo-wide grep for `datomic.codec` clean; battery green.
- [x] 3.3 Delete `modules/eacl-datomic/src/eacl/datomic/consistency.clj` and its test; in the same commit update `trusted_surface_audit_test.clj` (flip `derive-signing-key` from var-present to var-absent) AND remove `eacl.datomic.consistency` from the pinned namespace list in `bin/reflection-gate` (verified present at line 25). Verify: trusted-surface audit test green; `bin/reflection-gate` exits 0; repo-wide grep for `datomic.consistency` clean.
- [x] 3.4 Delete the private duplicate `relationship-seq` at `client/orchestration.cljc:1475`. Verify: kondo unused-private-var warning gone; battery green.
- [x] 3.5 Delete unreferenced vars: `direct_membership.cljc:12` `request-version`, `backend/source.cljc:31` `optional-source-operations`, `authorization/batch.cljc:252` `distinct-root-demands`, `datomic/backend.clj:31` `ordinary-view?`, `datomic/impl.clj:343` `relationship-relation-ids`. Keep `backend/v8.cljc:520` `maximum-concurrent-snapshot-read-width` (documented retained seam). Verify: `grep -nw` per deleted var over src+test+formal+bin returns nothing; battery green.
- [x] 3.6 Delete the 5 stale `exploration/stable-discovery/*.clj` duplicates of live `formal/stable-discovery` bridges (`cursor_refinement_bridge`, `progress_checkpoint_refinement_bridge`, `public_schema_refinement_bridge`, `randomized_refinement`, `sealed_plan_refinement_bridge`) — first checking `verify-fast.sh`'s header disposition and `formal/stable-discovery/README.md`: the parked concurrency models deliberately living only in `exploration/` (ReducerReadAhead, DescriptorCoalescing, ServiceLifecycle, ReadableWorkIndex, WeightedResponseLease, physical-scheduler bridge) are NOT deleted. Verify: `verify-fast.sh`'s bridge-manifest self-check still passes; grep confirms only `formal/` copies of the five remain; parked files untouched.
- [x] 3.7 Delete orphan test/bench namespaces with zero requirers and no runner/workflow reference: `eacl.report-queue`, `bench/version_comparison.clj`, `performance_amplification_reproduction.clj`, `operator_engine/{datomic_experiments,s3_experiments,union_performance}.clj`, `bench/{safe_retraction,datascript_relationship_storage}.clj`, `baseline/perf.clj`, `cache/standard_lru_test_runner.cljs`, `schema/expression_limit_calibration.clj`, `performance/release_win_cljs.cljs` — after re-verifying each has no reference from workflows, `bin/`, or docs. Verify: per-file grep clean before deletion; battery + the CLJS builds named in formal.yml still compile.

## 4. Hot-path check hoisting

- [x] 4.1 `engine/v8.cljc:1718-1722` and `:1725`: compute the cursor-scope digest and snapshot-proof identity once per page and reuse for every emitted row and the progress edge; apply the same to `recursive-operator-edge` (`:1142`) used at `:2004-2009`. Verify: cursor round-trip and pagination tests green; digests byte-identical before/after on a fixture page (assert in a temporary test or REPL check); battery green.
- [x] 4.2 Memoize `operator-cover-plan/seal-plan` results in the `*schema-cache*` `:sealed-plans` partition (keyed like `stable-plan`; memo key `[permission root]` plus the operator-plan fingerprint, inside the schema-identity-scoped partition) so `operator-lookup-page`, the recursive page path, and each `recursive-operator-count` page reuse one seal. Legality rests on the verified fact that the sealed output is closure-free data (design D3). Verify: operator lookup/count suites green; counterexample replay green; a REPL counter shows one seal per (schema identity, plan, permission) rather than per page.
- [x] 4.3 Seal plan-invariant derivations into the operator plan at `operator/plan.cljc:547-611`: `expression-roots` (replacing rebuilds at `lookup.cljc:114,159`, `vector_evaluator.cljc:148,372`, `recursive.cljc:1009`, `cover_plan.cljc:41,194`, and `evaluator.cljc:86-89` `plan-index`) and `acyclic-plan?`. Confirm digest input keys (`cursor_scope.cljc:10-35`) exclude the new fields. Verify: operator suites + cursor suites green; digest fixture check from 4.1 still byte-identical.
- [x] 4.4 `operator/evaluator.cljc:186-217`: hoist `normalize-limits` (precompute `known-limit-keys` as a def), invoker construction, and the per-candidate plan checks out of `check-eids`'s per-candidate call path from `vector_evaluator.cljc:245-261`. Verify: evaluator/vector-evaluator suites green; mutation-control test green (`operator-evaluator` with-redefs targets unmoved).
- [x] 4.5 Eliminate the 2–3× candidate normalization: introduce an internal already-normalized entry so `check-cached-many-eids` (`vector_evaluator.cljc:369`) normalizes once and `check-many-eids` (`:137`) plus the misses path (`:394`) trust it. Verify: vector-evaluator suites green including invalid-input tests (public entry still rejects malformed candidates with `:eacl.operator/invalid-vector`).
- [x] 4.6 `backend/direct_membership.cljc`: the per-probe `normalize-request` call (result discarded) deliberately validates "before cache state can influence work" (author comment) — so do NOT move validation after `group-by`. Instead replace it with an allocation-free inline `valid-probe!` running the same predicates directly (no synthetic singleton request map, no `set`/`distinct` allocations), preserving the validate-before-cache ordering and error identity exactly. Verify: direct-membership suites green including invalid-probe error tests; `eacl.formal.mutation-control-test` green (`direct/dispatch` is a registry consumer symbol).
- [x] 4.7 `operator/bitmask.cljc`: remove `check-index!` from `set-bit!`/`clear-bit!`/`bit-set?`/`indexes` (width validated at construction; indexes derive from `(range width)`); fix `empty?` (`:80-81`) to a non-allocating scan; stop re-validating width in `word-count` per `native` call. Verify: bitmask unit tests green incl. CLJS branch; vector-evaluator suites green.
- [x] 4.8 `execution/check!` call sites (`evaluator.cljc:103,111,298`; `recursive.cljc:302,834`; `seekable.cljc:74`; `direct_membership.cljc:148`; `permission_tree.cljc:263-265,411,703`): stop building `consumed-work` maps on the success path (thunk or nil-guard) and remove the duplicate second `check!` per permission-tree iteration; capture the clock fn once into the normalized contract at `execution/normalize` instead of a dynamic-var deref per read. Do NOT change sampling frequency. Before landing the capture, sweep all test files binding `*monotonic-nanos*` (ten files: execution, cache, subproblem-cache, permission-tree, datascript contract/batch/stable-page/physical-route/lookup, datomic recursive-cache tests) and fix any that create the contract outside the binding — the verified pattern binds at `normalize`. Verify: those suites green; deadline/cancellation tests green (exception `ex-data` still carries `consumed-work` on throw); battery green.
- [x] 4.9 `exact_integer` call-site collapse in `backend/v8.cljc` guards: run ONE combined predicate on the hot path. The failure branch (cold) reports whatever the executable tests actually assert — first read `runtime-guards-reject-negative-internal-eids-test` and the guard tests: if they pin the `:exact-integer`/`:nonnegative` distinction, classify on the failure branch; if not, report one honest obligation value and update the descriptive (existence-checked, never-parsed) `EACL-FORMAL-027/expected.edn` to match. Docs follow code. Verify: guard tests + the EACL-FORMAL-027 closing regression green; counterexample replay green.
- [x] 4.10 `backend/v8.cljc:865-879` `invoke`: fixed arities, resolve `adapter?`/`operation`/`runtime-guards?` once (currently `adapter?` runs twice per call), remove `& args`+`apply` and the `(last args)` O(n) recovery at `:706`/`:948`. Verify: backend suites green across all four adapters; battery green.
- [x] 4.11 Batch-check path `client/orchestration.cljc:1630-1659` (`counters-fn` runs up to 3× per item): read the consumed counter slots directly from the ledger instead of 4× 20-entry `snapshot`/`delta` maps, keeping (a) the per-request regression check at request level and (b) a production call to `batch/aggregate-counters` for the request-level report — it is a mutation-registry `with-redefs` target whose consumer claim must stay true. Replace `memoized!` with `memoized-active-state!` (verified: same body behind validation) with `active-state` resolved once per batch. Verify: batch API tests + counter/ledger tests green; `check-aggregate-limits!` and `throw-demand-error!` still receive the counter shapes their ex-data contract expects; `eacl.formal.mutation-control-test` green.
- [x] 4.12 `engine/least_path.cljc` `derives?` → `stable_route` membership probes: build the probe context (closures over plan indexes, reverse-rules, observers) once per least-path run in `make-context` instead of per witness probe (per probe today: 5-key `assoc` onto route-opts, `:pre` re-checks, `get-in` for reverse-rules, a volatile plus eight closures). Moderate-risk refactor — behavior identical, structure changes. Verify: least-path + stable-route suites green; membership-probe point-check suites green; witness/derivation results byte-identical on fixtures. (Note: `invoke-kernel`'s `normalize-selection` needs NO change — it already fast-paths the production shape; verified.)

## 5. Idiomatic and allocation fixes

- [x] 5.1 `operator/recursive.cljc`: pre-sort each `consumers` bucket when `component-consumers` builds the map (removing `pr-str`-keyed `sort-by` at `:863-866` from the fact-dequeue loop); fix the remaining `pr-str`/expensive-key `sort-by` at `:117`, `:881`, `:947` via decorate-sort-undecorate. Verify: recursive-operator suites green; result ordering byte-identical on fixtures.
- [x] 5.2 Apply `sealed-plan/sort-by-canonical`'s pattern to `cache.cljc:781`, `subproblem_cache.cljc:511,521,646`, `backend/direct_membership.cljc:253,265`. Verify: cache + direct-membership suites green; ordering unchanged on fixtures.
- [x] 5.3 `operator/vector_evaluator.cljc`: hoist the `(vec (repeat width unresolved))` get-default (`:161,:172`) to a loop-invariant binding; deref `@active` once per pass (`:190-193,:326-329`); compute `semantic-candidate-key` once per candidate (`:399,:406,:415`); replace `(count (filter :cached? …))` (`:416`, also `lookup.cljc:375`, `recursive.cljc:1234`) with arithmetic on known counts; `map-indexed` at `:81`. Verify: vector-evaluator suites green.
- [x] 5.4 Convert single-thread request-local atoms to volatiles per design D9: `vector_evaluator.cljc:145-150`, `permission_tree.cljc:256-262`. Verify: battery green; grep confirms no converted reference escapes to a store/observer.
- [x] 5.5 `engine/stable_reducer.cljc`: collapse the byte-identical `#?(:clj :cljs)` `ReducerState` constructor branches (`:63-86`); `(zero? (count …))` for vector emptiness at `:806` and `stable_route.cljc:249`, `stable_reducer.cljc:467`; fix `retain-buffer`'s 7-chained-assoc record rebuild (`:361-387`) with a single-constructor sibling; simplify the double negation at `:833`; single-pass `(vec (sort-by …))` at `:340-344`. The `stable-reducer/{finish,history-free,schedule,work-id}` vars are mutation-registry `with-redefs` targets — refactor around them, never rename or inline them. Verify: stable-reducer/stable-route suites green; `eacl.formal.mutation-control-test` green.
- [x] 5.6 Idiom sweep: `(not (empty? …))`→`seq` (`stable_reducer.cljc:454`, `cache.cljc:942`, `orchestration.cljc:3921`); `reverse`→`rseq` (`permission_tree.cljc:229-231`); `cons`→`subvec` consistency (`seekable.cljc:187`); `(conj heads driver-head)`+`apply =`→`every?` (`seekable.cljc:162-163,195`); hand-rolled memoize idiom at `permission_tree.cljc:269-270,303-304,325-326` and `engine/v8.cljc:557-558`; hand-rolled `count` and redundant `seq` in `cache/standard_lru.cljc:281,271`; O(n²) duplicate scan in `spicedb/parser.cljc:202-217` and `datomic/impl.clj:108` → set-based; `(vec coords)` 3× + `last`/`butlast` → `peek`/`pop` (`least_path.cljc:1022-1029`); dead `(declare head)` (`seekable.cljc:118`); pointless `result` binding (`physical.cljc:66-84`). Verify: battery green after each file's edits.
- [x] 5.7 Deduplicate helper copies: one shared stats/error helper namespace for the 5 copies of `add-stat!`, 5 of `invalid!`, 3 of `limit!` — without moving any mutation with-redefs target var (check the 23-target list first); dedup the 3-copy stats-reporting reducer (`stable_reducer.cljc:887-903`, `stable_route.cljc:232-246`, `engine/v8.cljc:1438-1455`) and the twin fetch-fns (`v8.cljc:1400-1421`) and twin count fns' shared option `select-keys` vectors (`stable_route.cljc:389-463`). Verify: `bin/formal source-closure` green (new ns joins closure); mutation-control test green; battery green.
- [x] 5.8 Namespace hygiene: remove stale `:refer-clojure :exclude` entries (`execution.cljc:8`, `subproblem_cache.cljc:8`, `request/counters.cljc:8`); fix core-shadowing local bindings where mechanical (`keys` in `vector_evaluator.cljc:37`, `next` at `:163`, `identity` bindings in `stable_reducer`/`cursor`/`subproblem_cache`); rename the `stable_reducer` `*aggregate-work-stats*` (`:875`) to a distinct name after confirming it is absent from the 93-root and with-redefs lists, updating its test references in the same commit. Verify: battery + source-closure green; grep shows one var per name.
- [x] 5.9 `cursor.cljc`: cache the encode `SecretKeySpec` like `pooled-authenticator` already does (`:289-296` vs `:355-358`); avoid boxing the 32-byte MAC into a persistent vector (`:348`) where comparison can run on bytes. Verify: cursor auth round-trip + tamper tests green (constant-time comparison property preserved).
- [x] 5.10 `execution.cljc`: make `expired?` 0-arity return a boolean (`:277-281`); fold the shared forbidden-key/cancellation checks of `normalize`/`refine` (`:173-189` vs `:221-249`) into one helper. Verify: execution tests green.
- [x] 5.11 Loop-invariant micro-hoists (each verified as invariant): `operator/evaluator.cljc` — hoist `(:predicate-programs plan)` lookup out of the transition loop (`:319`), define `known-limit-keys` once instead of `(set (keys default-limits))` twice per `normalize-limits`, destructure `limits` once instead of 10 re-lookups; `operator/vector_evaluator.cljc` — same `predicate-programs` hoist inside `evaluate!` (`:196-198`); `operator/seekable.cljc` — destructure the ≤4 hot option keys once per scan instead of per element; `backend/direct_membership.cljc:162-168` — merge the 7 consecutive `add-stat!` calls (three add zero) into one update; `engine/stable_route.cljc:118-119` — reuse `reducer/bounded-vector` instead of `(into [] (take …) …)`. Verify: operator + engine suites green; battery green.
- [x] 5.12 Bounded second-pass sweep of the namespaces the survey did not deep-audit for idioms — `schema/*.cljc`, `secure_format.cljc`, `relay.cljc`, `spicedb/parser.cljc` (remainder), `verified_kernel.cljc` (remainder), and `client/orchestration.cljc` (remainder) — using the same checklist (hot-path allocation, expensive sort keys, hand-rolled core fns, shadowing, stale excludes); fix what qualifies under this change's rules or record an explicit clean-bill/deferral per namespace. Verify: a per-namespace disposition note exists in the change dir; battery green after any fixes.

## 6. Adapter modules

- [x] 6.1 Extract shared scan-option normalization into `eacl.backend.v8` (typed error replacing the `case direction :asc nil :desc nil` throw-only idiom), hoist the per-call `(comp #(nth % 3) :v)` composition to a namespace-level def, and collapse each adapter's forward/reverse scan pair into one parameterized fn — for datascript, datahike, datalevin, datomic. PRESERVE each adapter's realization semantics exactly: datascript's non-empty scans stay lazy (caller-bounded — the eager-empty-only rationale is documented in the source) and Datalevin's stay eagerly realized inside `with-db` (LMDB snapshot scope). Align only the empty-case divergence (datahike returns the lazy result where datascript returns `[]`) to the eager-empty convention. Verify: each adapter's suite green (`:datalevin-test` for Datalevin — skip and report if the local `../datalevin` checkout is absent); cross-backend conformance suites green.
- [x] 6.2 Remove Datalevin's redundant per-element `exact-natural!` from its scan and direct-match operations, now covered by engine `guard-scan!` — but KEEP eager realization inside `ddb/with-db` (replace `(mapv exact-natural! …)` with `vec`, never with a lazy seq: results must materialize before the LMDB snapshot scope closes). Note the corrupt-data error-type change (design D5). Verify: Datalevin suite green including a corrupt-value test asserting the typed contract violation; no lazy escape from `with-db` (REPL probe on a closed-snapshot access).
- [x] 6.3 Fix the operator cover path double-guard: `cover_plan.cljc:173` must not re-enable `:runtime-guards?` on the wrapper when inner invokers already guard (`:119-128`). Verify: guard invocation counted once per tuple in a REPL probe; operator suites green.

## 7. Kernel boundary

- [x] 7.1 Remove `invoke-kernel`'s `expected-*` re-derivations and dead-operation compare branches (`verified_kernel.cljc:2211-2329`); KEEP the O(1) live-page bounds checks (`:2192-2209`), `validate-input!`/`validate-result!`, and `record-kernel-crossing!` untouched; delete the now-unreferenced `expected-*` fns. `normalize-selection` stays as-is (fast path verified). Verify: differential nREPL suites (`production-kernel`, `state-trace-differential`, `operator-decision`, `semantics-bridge`, `page-window-bridge`, `wire-format-bridge`, `java-round-trip`, `cross-runtime-vector`) green on the `:formal-smoke` classpath; battery green; kernel-crossing count gates green.
- [x] 7.2 Immediately after 7.1, run `eacl.formal.mutation-control-test` and confirm all 15 `portable/decide` controls still kill via the adjacent-literal comparison path (the boundary-rejection kill path narrowed when the oracle left; the literal path must carry each kill until 8.2 re-grounds them). Verify: mutation-control test green with unchanged required score; any control that stopped killing is fixed in this task, not deferred.
- [x] 7.3 Confirm no production namespace change altered the load-time formal coupling: `generated_runtime` load guard intact, kernel installation unchanged. Verify: cold `require` of `eacl.client.orchestration` succeeds against built `target/formal/java/classes`; `verified-authority-suite` cutover assertions green.

## 8. Formal harness falsifiability

- [x] 8.1 Make the source-closure selftest falsifiable: the tautology is in the `mode === "selftest"` branch of `bin/public-source-closure.mjs` (grep `:reintroduced` — it asserts a synthesized string contains the token it was built from). Replace with a negative/positive pair exercised through the real scan logic. Verify: selftest fails when the detection logic is deliberately broken locally, passes on the real tree.
- [x] 8.2 Strengthen `argument-ignoring-constant-fn?` (`mutation_control_test.clj:60-74`) to reject any argument-ignoring constant binding (named params included); fix all 15 literal-decidable controls in `executed_mutation_controls.cljc` to execute the real detector gate. Verify: the strengthened guard fails against the old control style (demonstrate once locally), mutation-control test green after the rewrite.
- [x] 8.3 `bin/formal apalache-mutation-control` (`bin/formal:430-456`): fail when the registry yields zero controls; assert the expected count from `registry.edn`. Verify: empty-set simulation exits non-zero; real run green.
- [x] 8.4 Counterexample replay: add `:formal-smoke` to the parity job nREPL alias (`formal.yml:565`) and assert zero skipped entries (`counterexample_replay_test.clj` gains `(= (count entries) (count available))` under a full-classpath flag). Update stale ledger references (`EACL-FORMAL-050` closing evidence naming a deleted test/file) or annotate them as historical. Verify: replay reports 67/67 resolved; characterization fixture updated for the YAML edit in the same commit and green.
- [x] 8.5 Make the generated-boundary reflection test fail loudly when generated classes are absent in CI context (env-gated) instead of passing with zero assertions (`characterization_fixture_test.clj:182-188`); give `verified-authority-suite/run-heavy!` a real required-operations set or a documented rationale for `#{}` (`verified_authority_suite.clj:219-221`). Verify: with classes absent and the CI flag set, the test fails; full run green.
- [x] 8.6 Correct stale `assurance_contract.clj` `:entry-points` (symbols with no definition: `run-generated-traversal`, `checkpoint-put!`, `frontier-permission-paths`, `traversal-permission?`, the five recursive/denotation names) and add a gate assertion that every listed entry-point resolves. Verify: `dafny-cleanup-gate` + the new resolution assertion green.

## 9. Formal harness and CI speed

- [x] 9.1 Deduplicate `compile-js`+`npm ci`: `browser-bundle` reuses `build-js` output (`bin/formal:216-230`); confirm `EACL-FORMAL-031`'s determinism expectation still holds. Verify: `bin/formal build-js browser-bundle` produces the same bundle digest as before, once each.
- [x] 9.2 Parallelize the 49 Dafny verifies (`bin/formal:171-190`) preserving one CSV per file (per-basename `:minimum-proof-efforts` mapping intact). Verify: local run produces identical CSV set; wall time drops; proof-effort ratchet comparison unchanged.
- [x] 9.3 Fix harness I/O waste: hoist `slurp` out of the marker loop (`dafny_cleanup_gate_test.clj:59-63`, currently 7 reads/file), single tree read in `public_source_closure_test.clj:84-90`, index the `inlineDefinitionSpans` lookup in `bin/public-source-closure.mjs:307-320`. Verify: tests green; timed runs faster.
- [x] 9.4 Move the routing-certificate benchmark (gates the production-dead `:recursive-routing-certificate`) from per-push formal.yml to the weekly cron; update `characterization_fixture_test.clj` pinned step assertions in the same commit. Verify: formal.yml valid, characterization test green, cron workflow lists the benchmark.
- [x] 9.5 Add `formal/stable-discovery/verify-fast.sh` (shipped engine's 651-obligation gate, currently unexecuted by CI) to the weekly cron; replace its 12 s wall-clock hard fail with a generous CI-mode limit; refresh its pinned obligation count/fingerprint if stale. Verify: script passes locally end-to-end (or its failure is reported with exact cause); cron workflow includes it.
- [x] 9.6 Investigate the `target/formal/**` vs `target/generated/**` generated-artifact path divergence between root and module `deps.edn`; align or document as intentional. Verify: a comment or fix exists; module builds green.

## 10. Final certification

- [x] 10.1 Full battery green via nREPL, including the DataScript CLJS build run last (per AGENTS.md shutdown-agents caveat). Verify: 0 failures/errors, counts compared to the 1.2 baseline.
- [x] 10.2 Formal gate sweep: `bin/formal source-closure`, `format`, `build-java`, `build-js`, `browser-bundle`; the eight differential nREPL suites; `eacl.formal.{generators,cache-strategy-adversarial,mutation-control}-test`; counterexample replay 67/67; `bin/reflection-gate`. Verify: all exit 0.
- [x] 10.3 Performance evidence: re-run the 1.4 baselines; deterministic-work and benchmark gates at or below baseline on every touched operation shape (expected: strictly less work on hoisted paths). Verify: comparison recorded in the change dir; no gate regression.
- [x] 10.4 `openspec validate --change cleanup-and-simplify-v8-core` passes and all tasks above are checked. Verify: validate exits 0.
