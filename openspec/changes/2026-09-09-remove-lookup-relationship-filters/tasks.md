> **Superseded relationship-read requirements (2026-09-09):** The later `2026-09-09-remove-relationship-read-authorization` change replaces every requirement below to retain, construct, validate or migrate to `read-relationships :authorization`. Nested demo branches use plain indexed reads with the exact parent/type/relation, pagination, consistency and cache controls, and perform no endpoint permission checks. Removed authorization fields are rejected on presence, including nil/null. Viewing subject and permission remain inputs to root lookups, the access inspector and permission checks only. Library callers needing endpoint authorization compose direct reads with `can?` or `check-permissions` on one snapshot and own filtered pagination. Historical completed tasks and measurements below describe the earlier release, not the new contract. All unrelated requirements remain in force. Apply the companion delta before archiving; do not restore the superseded clauses.

## 1. Establish Implementation Boundaries

- [ ] 1.1 Refresh the two-key caller inventory against the current core checkout and known consumers; verify with `rg` and record which matches implement the API, call it, test it or document historical behavior.
- [ ] 1.2 Trace external predicate plumbing and internal recursive/operator/qualified consumers before deletion; verify a written keep/remove map for shared engine helpers and backend invokers.
- [ ] 1.3 Capture deterministic pre-removal cursors for both filtered lookup directions in an isolated compatibility harness; verify the harness produces valid continuations before using them to test rejection on the changed build.
- [ ] 1.4 Establish the installed/published module versions and known consumer pins relevant to release; verify migration notes distinguish inspected evidence from unknown external adoption.
- [ ] 1.5 Reconcile the current demo revision with the review evidence (`1693f04`/`77bf776` already migrated nested routing); verify the remaining wire constructors, bounded metadata, Jank caller and artifacts without overwriting concurrent work.

## 2. Remove the Public Lookup Contract

- [ ] 2.1 Add admission tests for both keys on both lookups and both count operations, valid/nil/false/malformed values and both-key priority across public facades, clients and supported retained/speculative views; verify typed errors, no new snapshot/cache/backend/lookahead work, caller-owned snapshot survival and preserved invalid-target/lifecycle errors.
- [ ] 2.2 Implement the explicit unsupported-filter admission behavior early enough to precede snapshot-read preparation and remove both keys from accepted lookup envelopes; verify tests from 2.1 through nREPL, retaining ordinary unknown-key behavior and making no claim that count filters previously worked.
- [ ] 2.3 Remove filter-only orchestration, clause schema validation, nested-anchor normalization and conditional compute/continuation routing; verify ordinary forward/reverse page tests and search results showing no executable public filter dispatch.
- [ ] 2.4 Prune only unreachable external predicate plumbing identified in 1.2; verify direct, recursive, operator and qualified lookup tests preserve supported order, membership, evidence and bounded traversal.
- [ ] 2.5 Verify historical filtered cursors, including empty progress cursors, cannot resume plain lookups or relationship reads with caches enabled and bypassed; verify ordinary pre-removal cursor continuation and unchanged scope/ABI. If the expected isolation fails, correct the defect or explicitly revise the plan before any incompatible ABI change.
- [ ] 2.6 Retain branch relationship dependencies in authorized-read cache/cursor proofs; verify independent branch-membership and permission mutations under supported exact/live/freshness modes, including exact-basis fallback on backends without complete proofs.

## 3. Preserve Tests and Performance Safeguards

- [ ] 3.1 Update shared aggregate contract support and DataScript batch tests to retain scan/batch assertions while replacing enumerate cases with public rejection coverage; verify all supported backend consumers of the shared contract pass.
- [ ] 3.2 Split mixed qualified/operator tests, preserving unfiltered policies, evidence, forward/backward pages and cache assertions; verify DataScript, Datomic and Datalevin targeted suites pass through nREPL.
- [ ] 3.3 Remove enumerate-only Datalevin benchmark arms and thresholds without removing scalar-vs-scan/batch comparisons; verify retained counters for shared context, preparation and zero nested public calls still pass.
- [ ] 3.4 Reconcile formal mutation consumers and filtered-pagination assurance mappings with reachable retained operations; verify the relevant formal/mutation commands pass and no deleted test/function references remain.
- [ ] 3.5 Run required core/backend JVM tests through nREPL, then the applicable CLJS suite last and `bin/formal source-closure`; verify pass reports under ignored output directories and preserve unrelated changes.
- [ ] 3.6 Exercise page size 1, candidate budget 1, budget equal to page size, dense/sparse/all-denied pages, exact exhaustion, accepted sentinel and reverse traversal; verify concatenated accepted output against an independent fixture oracle with no omission/duplication and correct opaque progress cursors.
- [ ] 3.7 Preserve ordinary qualified lookups and stored/expiry-active relationship-read semantics; verify explicit expired-parent, caveat-false-parent and conditional-only permission counterexamples cannot be presented as automatic conversion to an equivalent Boolean authorized read.

## 4. Coordinate the Companion Demo Migration

- [ ] 4.1 Create/link an `eacl-demo` companion change in that repository's planning scope, covering browser, four JVM profiles, wire validation and vendored Jank; verify its requirements supersede the nested filtered-lookup mandate. Do not modify sibling source under this core-local apply scope.
- [ ] 4.2 Obtain companion evidence that nested branches use indexed authorized relationship reads with exact type/relation/anchor and `:on :resource`, forwarding consistency/cache controls; verify complete authorization/resource-type wire admission, rejection of old lookup field triplets and partial/nil variants, and traces containing no public per-row check loop or full fixture scan.
- [ ] 4.3 Obtain companion fixture tests for unqualified unique endpoint mapping at generation/seed boundaries (small/large cuts and supported larger browser seeds), stable relationship ordering, cursor recovery and short/empty bounded pages; verify selected subject/resource and permission persist, bounded metadata reaches the UI, and Next continues an empty bounded window without a production full-dataset validation scan.
- [ ] 4.4 Obtain companion UI evidence that latency/cache badges cover the complete query, cumulative accepted ranges handle short pages, root counts are not mislabeled as branch totals, and superseded responses cannot overwrite current scope; verify DataScript and representative remote-backend browser tests.
- [ ] 4.5 Obtain Jank evidence that the filter exemplar and vendored validation use/reject the intended APIs consistently; verify its fixture verification executes successfully against the packaged replacement.
- [ ] 4.6 Set operational measurement budgets before running the companion comparison, then record broad-user/narrow-branch and sparse-user/platform-wide cases using measured fixture cardinalities with candidate/backend work plus cold/warm/bypass latency; isolate foreground work with lookahead disabled, separately verify configured background depth/concurrency, and account for exhaustion probes instead of treating candidate budget as total I/O. Report regressions without raising thresholds after measurement.

## 5. Reconcile Documentation and Qualify Release

- [ ] 5.1 Update aggregate authorization docs and applicable README references to remove both public clauses and explain explicit authorized-read migration, row/order differences and obsolete cursor rejection; document why reverse-filter callers need bounded same-snapshot relationship reads plus batch checks rather than an inverted `:on`. Verify live examples contain no removed API invocation and test any actual discovered reverse-filter consumer against its scalar oracle without creating unused replacement APIs.
- [ ] 5.2 Reconcile superseded enumerate-route requirements in active core changes without undoing batch/scan requirements; verify a search distinguishes historical reports from current promises and companion demo spec reconciliation is linked.
- [ ] 5.3 Verify isolated packaged backend modules and clean-consumer builds reject obsolete filters and retain ordinary APIs; record a compatible core/browser/JVM/Jank version matrix before release adoption.
- [ ] 5.4 Test old-browser/new-service, migrated-browser/compatible-old-service and migrated-browser/new-service combinations against packaged artifacts; verify no ignored authorization fields or raw-read fallback, explicit cursor/validation recovery, and no schema/data migration.
- [ ] 5.5 Run strict OpenSpec validation and review each normative scenario against test or companion evidence; verify no implementation task is marked complete without its stated evidence. Publication/deployment remains a separate release action.
