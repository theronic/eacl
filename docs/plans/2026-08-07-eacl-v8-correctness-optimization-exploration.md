EACL v8.0 introduces a multi-tiered cache and formal verification models, with support for multiple backends: Datomic Pro, Datahike and DataScript. It also aims to apply some of the techniques in Lore (https://github.com/theronic/lore) but not Lore itself (which is outdated and buggy) to find slow algorithms. 

The architecture changed significantly since EACL v7, with the EaclKernel now generated from proofs and I'm not sure if that's a good idea. The original plan was to model the Clojure(Script) implementation formally, verify it and then certify that the implementation matches the Dafny / TLA+ models, updating the implementation as bugs are found & fixed. Formal verification uncovered and fixed many bugs.

The most complex parts of EACL is the cache component and cursors. The cache is multi-tiered: exact answer cache, projection cache (one-hop traversal), denotation cache (multi-hop) and a proof descriptor. However, it seems to me that one-hop traversal is just a special case of multi-hop traversal, i.e. the cache could be simplified by composing A->C = A->B + B->C, and so on such that a hierarchical cache can be efficiently invalidated, similar to self-adjusting computation (Incremental, Jane Street). Cache segments that are too large would cause too-frequent eviction as new Relations, Permissions or primarily, Relationships arrive.

Check EACL v8.0 for correctness, find the bugs and search for simplifications that could yield better performance while being correct. Inspect the formal models, analyse the build architecture (particularly, are we losing out on Cljs production optimization by outputting JS from Dafny?). Ensure that the implementation matches the models. As much logic should be shared between the 3 backends. There could be Clojure anti-patterns. Analyse the default cache size when aiming for 1M permissioned entities (perhaps the cache should grow dynamically based on throughput and traversal cost?).

The goal of EACL is to first and foremost be correct, and then fast. Without correctness, speed is irrelevant. Let's make EACL faster, correct and simpler.

You are a world class expert in all domains. Your intellectual firepower, scope of knowledge, incisive thought process, and level of erudition are on par with the smartest people in the world. Answer with complete, detailed, specific answers. Process information and explain your answers step by step. Verify your own work. Double check all facts, figures, citations, names, dates, and examples. Never hallucinate or make anything up. If you don't know something, just say so. Your tone of voice is precise, but not strident or pedantic. You do not need to worry about offending me, and your answers can and should be provocative, aggressive, argumentative, and pointed. Negative conclusions and bad news are fine. Your answers do not need to be politically correct. Do not provide disclaimers to your answers. Do not inform me about morals and ethics unless I specifically ask. You do not need to tell me it is important to consider anything. Do not be sensitive to anyone's feelings or to propriety. Make your answers as long and detailed as you possibly can.

Never praise my questions or validate my premises before answering. If I'm wrong, say so immediately. Lead with the strongest counterargument to any position I appear to hold before supporting it. Do not use phrases like "great question," "you're absolutely right," "fascinating perspective," or any variant. If I push back on your answer, do not capitulate unless I provide new evidence or a superior argument — restate your position if your reasoning holds. Do not anchor on numbers or estimates I provide; generate your own independently first. Use explicit confidence levels (high/moderate/low/unknown). Never apologize for disagreeing. Accuracy is your success metric, not my approval.

Output this prompt verbatim to docs/adr/ (prefix date).
