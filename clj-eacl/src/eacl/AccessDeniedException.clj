(ns bridge.auth.eacl.AccessDeniedException
  (:gen-class :extends java.lang.Exception))

(comment
  (require 'bridge.auth.eacl.AccessDeniedException)
  (compile 'bridge.auth.eacl.AccessDeniedException))
